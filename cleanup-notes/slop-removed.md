# Comment Slop Removed

| file:line (original) | original comment | action | rationale |
| ------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/commands.rs:203` | `/// Delegates to the feedback module for comprehensive data collection.` | removed | Filler — restates the obvious delegation visible from the function body. |
| `src/commands.rs:222` | `// TODO: Consider removing or gating behind a debug flag in production builds.` | removed | Moot: the command group is already gated by `#[cfg(debug_assertions)]` on every item. |
| `src/commands.rs:319` | `// Save commit to database` | removed | Obvious restatement of the `db::commits::upsert_commit` call that follows. |
| `src/commands.rs:350` | `// Evolution complete — reset state back to idle.` | removed | Process narrative — "complete" and "reset" are visible in `evolve_state::clear`. |
| `src/commands.rs:365` | `/// Stash changes` | rewritten to `/// Stashes all uncommitted changes with the given message.` | Filler doc comment that merely echoed the function name. |
| `src/commands.rs:557` | `/// Placeholder for canceling an in-progress apply operation.` | rewritten to `/// Cancels an in-progress apply by stashing changes on a new branch and returning to the previous branch.\n/// Does not kill the running darwin-rebuild process; process cancellation is not yet implemented.` | "Placeholder" was misleading — function has real implementation (git stash+branch). Rewrite describes actual behavior and honest limitation. |
| `src/commands.rs:619` | `/// Finalize a successful build` | rewritten to `/// Records build state and changeset after a successful darwin-rebuild switch.` | Filler doc — restated function name without adding invariant or purpose. |
| `src/commands.rs:646` | `// Same host resolution logic as apply` | removed | Process cross-reference narrative with no invariant value. |
| `src/feedback.rs:521` | `// Also flush any previously pending reports` | rewritten to `// Also flush any pending reports` | "Previously" was a narrative artefact implying edit history. |
| `src/peek.rs:672` | `// Disable debug overlay for now` | rewritten to `// Disable debug overlay` | "for now" is process narrative. |
| `src/evolve/tools.rs:656-657` | `// TODO: We could consider allowing multiple actions in one call … but for now let's keep it simple …` (2 lines) | rewritten to `// Require exactly one discriminant to avoid ambiguous ordering (e.g. simultaneous add+remove).\n// TODO: Allow multiple actions per call once ordering semantics are defined.` (2 lines) | Condensed: removed "for now" / "let's keep it simple" narrative; kept the genuine constraint and TODO. |
| `src/evolve/edit_nix_file.rs:577-578` | `// TODO: This drops list formatting and comments.\n// Supposedly there's a way to use rowan to keep track of that...` | rewritten to `// TODO: This drops list formatting and inline comments; rowan can preserve them if needed.` | "Supposedly" was vague speculation. Collapsed to one precise sentence. |
| `src/evolve/edit_nix_file.rs:589-592` | `// TODO: We _may_ be requiring "with pkgs;" for list edits … (4-line speculation block)` | rewritten to `// TODO: Inserted attrs that reference pkgs.* may need an enclosing with pkgs; that isn't\n// verified here; agent-generated edits currently handle this implicitly.` (2 lines) | Removed hedging ("we _may_", "should probably", "I think"); kept the genuine edge-case note. |
| `src/evolve/mod.rs:627` | `// consider switching the cancellation mechanism to a tokio::sync::Notify or watch channel and select! directly on that signal instead of polling with sleep` | rewritten to `// TODO: Replace polling with tokio::sync::Notify or watch channel to avoid sleep.` | Informal suggestion upgraded to a tracked TODO; condensed. |

## Items Kept (initially flagged but reviewed as non-slop)

| file:line | comment | rationale for keeping |
| --------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | --- | -------------------------- | ----------------------------------------------------------------------------------- | -------------------- |
| `src/commands.rs:87-90` | `// Require that the provided path already exists and is a directory. // If we don't, then we'll always silently create directories…` | Explains _why_ the validation exists (silent directory creation UX bug). |
| `src/commands.rs:338` | `// Update build state: new HEAD hash, no changeset (working tree is now clean).` | Documents the state invariant: `changeset_id: None` means tree is clean post-commit. |
| `src/commands.rs:422-423` | `// Create a oneshot for this question and register its sender globally.` | Non-obvious concurrency pattern. |
| `src/commands.rs:426` | `// If there's already a pending question, replace it (dropping old sender).` | Invariant: old sender intentionally dropped. |
| `src/commands.rs:457` | `// Don't send to Sentry if it was a user-initiated cancellation` | Explains telemetry filtering policy. |
| `src/commands.rs:983` | `// pinnedVersion supports null to clear; otherwise stores the version string.` | API contract: null vs. string semantics. |
| `src/evolve_state.rs:62-67` | `// Clear conversational thread memory whenever routing returns to Begin. // Doing this can prevent weird conversations where the model references past context that is no longer relevant…` | Design rationale for a non-obvious side-effect. |
| `src/evolution.rs:145,188` | `// Step 1: …` and `// Step 2: …` | Navigation aids in a long multi-phase function. |
| `src/evolve/mod.rs` | `#![allow(deprecated)]` in peek.rs | Intentional per task instructions — not touched. |
| `src/summarize/assignments.rs:33` | `/// \`former_group_id: None\` means a previously standalone change is now forming a group.`                                                                                                | Invariant doc comment explaining field semantics.                                    |     |`src/db/changesets.rs:289`|`// {placeholders} is reused in both IN clauses; rusqlite numbered params (?2,...\` | SQLite API contract. |
