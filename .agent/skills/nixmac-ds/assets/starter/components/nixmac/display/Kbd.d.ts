import type * as React from "react";
/** Keyboard-key cap for shortcut hints (⌘K, Esc, ↵). */
export interface KbdProps extends React.ComponentProps<"kbd"> {}
export function Kbd(props: KbdProps): React.JSX.Element;
