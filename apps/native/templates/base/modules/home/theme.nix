# Theme configuration (Catppuccin is configured in default.nix)
{ config, pkgs, lib, ... }:

{
  # Additional theming options can go here
  # The main catppuccin config is in default.nix

  # Example: Custom theme-related settings
  # gtk = { ... };
  # qt = { ... };
}

