Below are the changes to summarize together. Each entry includes:

- **hash** — unique key for the change
- **filename** — file path where the change occurred
- **lines** — affected line count
- **diff** — the patch hunk

Use these fields to infer one commit message for the whole diff. Do not invent broader intent than the visible hunks support.
