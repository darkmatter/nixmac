# launchd

<!-- AUTO-GENERATED by scripts/nix-options.sh. Do not edit by hand. -->

All options under `launchd`.

| Option | Type | Description |
| --- | --- | --- |
| `launchd.agents` | `attribute set of (submodule)` | Definition of per-user launchd agents. When a user logs in, a per-user launchd is started. It does the following: 1. It loads the parameters for each launch-on-demand user agent from the property list files found in /System/Library/LaunchAgents, /Library/LaunchAgents, and the user’s individual Library/LaunchAgents directory. 2. It registers the sockets and file descriptors requested by those user agents. 3. It launches any user agents that requested to be running all the time. 4. As requests for a particular service arrive, it launches the corresponding user agent and passes the request to it. 5. When the user logs out, it sends a SIGTERM signal to all of the user agents that it started. |
| `launchd.agents.<name>.command` | `string or absolute path` | Command executed as the service's main process. |
| `launchd.agents.<name>.environment` | `attribute set of (string or list of string)` | Environment variables passed to the service's processes. |
| `launchd.agents.<name>.path` | `list of (absolute path or string)` | Packages added to the service's {env}`PATH` environment variable. Only the {file}`bin` and subdirectories of each package is added. |
| `launchd.agents.<name>.script` | `strings concatenated with "\n"` | Shell commands executed as the service's main process. |
| `launchd.agents.<name>.serviceConfig` | `submodule` | Each attribute in this set specifies an option for a key in the plist. <https://developer.apple.com/legacy/library/documentation/Darwin/Reference/ManPages/man5/launchd.plist.5.html> |
| `launchd.agents.<name>.serviceConfig.AbandonProcessGroup` | `null or boolean` | When a job dies, launchd kills any remaining processes with the same process group ID as the job. Setting this key to true disables that behavior. |
| `launchd.agents.<name>.serviceConfig.Debug` | `null or boolean` | This optional key specifies that launchd should adjust its log mask temporarily to LOG_DEBUG while dealing with this job. |
| `launchd.agents.<name>.serviceConfig.Disabled` | `null or boolean` | This optional key is used as a hint to `launchctl(1)` that it should not submit this job to launchd when loading a job or jobs. The value of this key does NOT reflect the current state of the job on the running system. If you wish to know whether a job is loaded in launchd, reading this key from a configuration file yourself is not a sufficient test. You should query launchd for the presence of the job using the `launchctl(1)` list subcommand or use the ServiceManagement framework's `SMJobCopyDictionary()` method. Note that as of Mac OS X v10.6, this key's value in a configuration file conveys a default value, which is changed with the [-w] option of the `launchctl(1)` load and unload subcommands. These subcommands no longer modify the configuration file, so the value displayed in the configuration file is not necessarily the value that `launchctl(1)` will apply. See `launchctl(1)` for more information. Please also be mindful that you should only use this key if the provided on-demand and KeepAlive criteria are insufficient to describe the conditions under which your job needs to run. The cost to have a job loaded in launchd is negligible, so there is no harm in loading a job which only runs once or very rarely. |
| `launchd.agents.<name>.serviceConfig.EnableGlobbing` | `null or boolean` | This flag causes launchd to use the `glob(3)` mechanism to update the program arguments before invocation. |
| `launchd.agents.<name>.serviceConfig.EnableTransactions` | `null or boolean` | This flag instructs launchd that the job promises to use `vproc_transaction_begin(3)` and `vproc_transaction_end(3)` to track outstanding transactions that need to be reconciled before the process can safely terminate. If no outstanding transactions are in progress, then launchd is free to send the SIGKILL signal. |
| `launchd.agents.<name>.serviceConfig.EnvironmentVariables` | `null or (attribute set of string)` | This optional key is used to specify additional environment variables to be set before running the job. |
| `launchd.agents.<name>.serviceConfig.ExitTimeOut` | `null or signed integer` | The amount of time launchd waits before sending a SIGKILL signal. The default value is 20 seconds. The value zero is interpreted as infinity. |
| `launchd.agents.<name>.serviceConfig.GroupName` | `null or string` | This optional key specifies the group to run the job as. This key is only applicable when launchd is running as root. If UserName is set and GroupName is not, the the group will be set to the default group of the user. |
| `launchd.agents.<name>.serviceConfig.HardResourceLimits` | `null or (submodule)` | Resource limits to be imposed on the job. These adjust variables set with `setrlimit(2)`. The following keys apply: |
| `launchd.agents.<name>.serviceConfig.HardResourceLimits.CPU` | `null or signed integer` | The maximum amount of cpu time (in seconds) to be used by each process. |
| `launchd.agents.<name>.serviceConfig.HardResourceLimits.Core` | `null or signed integer` | The largest size (in bytes) core file that may be created. |
| `launchd.agents.<name>.serviceConfig.HardResourceLimits.Data` | `null or signed integer` | The maximum size (in bytes) of the data segment for a process; this defines how far a program may extend its break with the `sbrk(2)` system call. |
| `launchd.agents.<name>.serviceConfig.HardResourceLimits.FileSize` | `null or signed integer` | The largest size (in bytes) file that may be created. |
| `launchd.agents.<name>.serviceConfig.HardResourceLimits.MemoryLock` | `null or signed integer` | The maximum size (in bytes) which a process may lock into memory using the `mlock(2)` function. |
| `launchd.agents.<name>.serviceConfig.HardResourceLimits.NumberOfFiles` | `null or signed integer` | The maximum number of open files for this process. Setting this value in a system wide daemon will set the `sysctl(3)` kern.maxfiles (SoftResourceLimits) or kern.maxfilesperproc (HardResourceLimits) value in addition to the `setrlimit(2)` values. |
| `launchd.agents.<name>.serviceConfig.HardResourceLimits.NumberOfProcesses` | `null or signed integer` | The maximum number of simultaneous processes for this user id. Setting this value in a system wide daemon will set the `sysctl(3)` kern.maxproc (SoftResourceLimits) or kern.maxprocperuid (HardResourceLimits) value in addition to the `setrlimit(2)` values. |
| `launchd.agents.<name>.serviceConfig.HardResourceLimits.ResidentSetSize` | `null or signed integer` | The maximum size (in bytes) to which a process's resident set size may grow. This imposes a limit on the amount of physical memory to be given to a process; if memory is tight, the system will prefer to take memory from processes that are exceeding their declared resident set size. |
| `launchd.agents.<name>.serviceConfig.HardResourceLimits.Stack` | `null or signed integer` | The maximum size (in bytes) of the stack segment for a process; this defines how far a program's stack segment may be extended. Stack extension is performed automatically by the system. |
| `launchd.agents.<name>.serviceConfig.InitGroups` | `null or boolean` | This optional key specifies whether `initgroups(3)` should be called before running the job. The default is true in 10.5 and false in 10.4. This key will be ignored if the UserName key is not set. |
| `launchd.agents.<name>.serviceConfig.KeepAlive` | `null or boolean or (submodule)` | This optional key is used to control whether your job is to be kept continuously running or to let demand and conditions control the invocation. The default is false and therefore only demand will start the job. The value may be set to true to unconditionally keep the job alive. Alternatively, a dictionary of conditions may be specified to selectively control whether launchd keeps a job alive or not. If multiple keys are provided, launchd ORs them, thus providing maximum flexibility to the job to refine the logic and stall if necessary. If launchd finds no reason to restart the job, it falls back on demand based invocation. Jobs that exit quickly and frequently when configured to be kept alive will be throttled to converve system resources. |
| `launchd.agents.<name>.serviceConfig.Label` | `string` | This required key uniquely identifies the job to launchd. |
| `launchd.agents.<name>.serviceConfig.LaunchEvents` | `null or (attribute set)` | Specifies higher-level event types to be used as launch-on-demand event sources. Each sub-dictionary defines events for a particular event subsystem, such as "com.apple.iokit.matching", which can be used to launch jobs based on the appearance of nodes in the IORegistry. Each dictionary within the sub-dictionary specifies an event descriptor that is specified to each event subsystem. With this key, the job promises to use the xpc_set_event_stream_handler(3) API to consume events. See xpc_events(3) for more details on event sources. |
| `launchd.agents.<name>.serviceConfig.LaunchOnlyOnce` | `null or boolean` | This optional key specifies whether the job can only be run once and only once. In other words, if the job cannot be safely respawned without a full machine reboot, then set this key to be true. |
| `launchd.agents.<name>.serviceConfig.LimitLoadFromHosts` | `null or (list of string)` | This configuration file only applies to hosts NOT listed with this key. Note: One should set kern.hostname in `sysctl.conf(5)` for this feature to work reliably. |
| `launchd.agents.<name>.serviceConfig.LimitLoadToHosts` | `null or (list of string)` | This configuration file only applies to the hosts listed with this key. Note: One should set kern.hostname in `sysctl.conf(5)` for this feature to work reliably. |
| `launchd.agents.<name>.serviceConfig.LimitLoadToSessionType` | `null or string or list of string` | This configuration file only applies to sessions of the type specified. This key is used in concert with the -S flag to {command}`launchctl`. |
| `launchd.agents.<name>.serviceConfig.LowPriorityBackgroundIO` | `null or boolean` | This optional key specifies whether the kernel should consider this daemon to be low priority when doing file system I/O when the process is throttled with the Darwin-background classification. |
| `launchd.agents.<name>.serviceConfig.LowPriorityIO` | `null or boolean` | This optional key specifies whether the kernel should consider this daemon to be low priority when doing file system I/O. |
| `launchd.agents.<name>.serviceConfig.MachServices` | `null or (attribute set of (boolean or (submodule)))` | This optional key is used to specify Mach services to be registered with the Mach bootstrap sub-system. Each key in this dictionary should be the name of service to be advertised. The value of the key must be a boolean and set to true. Alternatively, a dictionary can be used instead of a simple true value. Finally, for the job itself, the values will be replaced with Mach ports at the time of check-in with launchd. |
| `launchd.agents.<name>.serviceConfig.Nice` | `null or signed integer` | This optional key specifies what nice(3) value should be applied to the daemon. |
| `launchd.agents.<name>.serviceConfig.OnDemand` | `null or boolean` | This key was used in Mac OS X 10.4 to control whether a job was kept alive or not. The default was true. This key has been deprecated and replaced in Mac OS X 10.5 and later with the more powerful KeepAlive option. |
| `launchd.agents.<name>.serviceConfig.ProcessType` | `null or one of "Background", "Standard", "Adaptive", "Interactive"` | This optional key describes, at a high level, the intended purpose of the job. The system will apply resource limits based on what kind of job it is. If left unspecified, the system will apply light resource limits to the job, throttling its CPU usage and I/O bandwidth. The following are valid values: Background : Background jobs are generally processes that do work that was not directly requested by the user. The resource limits applied to Background jobs are intended to prevent them from disrupting the user experience. Standard : Standard jobs are equivalent to no ProcessType being set. Adaptive : Adaptive jobs move between the Background and Interactive classifications based on activity over XPC connections. See `xpc_transaction_begin(3)` for details. Interactive : Interactive jobs run with the same resource limitations as apps, that is to say, none. Interactive jobs are critical to maintaining a responsive user experience, and this key should only be used if an app's ability to be responsive depends on it, and cannot be made Adaptive. |
| `launchd.agents.<name>.serviceConfig.Program` | `null or absolute path` | This key maps to the first argument of `execvp(3)`. If this key is missing, then the first element of the array of strings provided to the ProgramArguments will be used instead. This key is required in the absence of the ProgramArguments key. |
| `launchd.agents.<name>.serviceConfig.ProgramArguments` | `null or (list of string)` | This key maps to the second argument of `execvp(3)`. This key is required in the absence of the Program key. Please note: many people are confused by this key. Please read `execvp(3)` very carefully! |
| `launchd.agents.<name>.serviceConfig.QueueDirectories` | `null or (list of string)` | Much like the WatchPaths option, this key will watch the paths for modifications. The difference being that the job will only be started if the path is a directory and the directory is not empty. |
| `launchd.agents.<name>.serviceConfig.RootDirectory` | `null or string` | This optional key is used to specify a directory to `chroot(2)` to before running the job. |
| `launchd.agents.<name>.serviceConfig.RunAtLoad` | `null or boolean` | This optional key is used to control whether your job is launched once at the time the job is loaded. The default is false. |
| `launchd.agents.<name>.serviceConfig.ServiceIPC` | `null or boolean` | This optional key specifies whether the job participates in advanced communication with launchd. The default is false. This flag is incompatible with the inetdCompatibility key. |
| `launchd.agents.<name>.serviceConfig.SessionCreate` | `null or boolean` | This key specifies that the job should be spawned into a new security audit session rather than the default session for the context is belongs to. See auditon(2) for details. |
| `launchd.agents.<name>.serviceConfig.Sockets` | `null or (attribute set of (submodule))` | This optional key is used to specify launch on demand sockets that can be used to let launchd know when to run the job. The job must check-in to get a copy of the file descriptors using APIs outlined in launch(3). The keys of the top level Sockets dictionary can be anything. They are meant for the application developer to use to differentiate which descriptors correspond to which application level protocols (e.g. http vs. ftp vs. DNS...). At check-in time, the value of each Sockets dictionary key will be an array of descriptors. Daemon/Agent writers should consider all descriptors of a given key to be to be effectively equivalent, even though each file descriptor likely represents a different networking protocol which conforms to the criteria specified in the job configuration file. The parameters below are used as inputs to call `getaddrinfo(3)`. |
| `launchd.agents.<name>.serviceConfig.Sockets.<name>.Bonjour` | `null or boolean or list of string` | This optional key can be used to request that the service be registered with the `mDNSResponder(8)`. If the value is boolean, the service name is inferred from the SockServiceName. |
| `launchd.agents.<name>.serviceConfig.Sockets.<name>.MulticastGroup` | `null or string` | This optional key can be used to request that the datagram socket join a multicast group. If the value is a hostname, then `getaddrinfo(3)` will be used to join the correct multicast address for a given socket family. If an explicit IPv4 or IPv6 address is given, it is required that the SockFamily family also be set, otherwise the results are undefined. |
| `launchd.agents.<name>.serviceConfig.Sockets.<name>.SecureSocketWithKey` | `null or string` | This optional key is a variant of SockPathName. Instead of binding to a known path, a securely generated socket is created and the path is assigned to the environment variable that is inherited by all jobs spawned by launchd. |
| `launchd.agents.<name>.serviceConfig.Sockets.<name>.SockFamily` | `null or one of "IPv4", "IPv6"` | This optional key can be used to specifically request that "IPv4" or "IPv6" socket(s) be created. |
| `launchd.agents.<name>.serviceConfig.Sockets.<name>.SockNodeName` | `null or string` | This optional key specifies the node to `connect(2)` or `bind(2)` to. |
| `launchd.agents.<name>.serviceConfig.Sockets.<name>.SockPassive` | `null or boolean` | This optional key specifies whether `listen(2)` or `connect(2)` should be called on the created file descriptor. The default is true ("to listen"). |
| `launchd.agents.<name>.serviceConfig.Sockets.<name>.SockPathMode` | `null or signed integer` | This optional key specifies the mode of the socket. Known bug: Property lists don't support octal, so please convert the value to decimal. |
| `launchd.agents.<name>.serviceConfig.Sockets.<name>.SockPathName` | `null or absolute path` | This optional key implies SockFamily is set to "Unix". It specifies the path to `connect(2)` or `bind(2)` to. |
| `launchd.agents.<name>.serviceConfig.Sockets.<name>.SockProtocol` | `null or value "TCP" (singular enum)` | This optional key specifies the protocol to be passed to `socket(2)`. The only value understood by this key at the moment is "TCP". |
| `launchd.agents.<name>.serviceConfig.Sockets.<name>.SockServiceName` | `null or string` | This optional key specifies the service on the node to `connect(2)` or `bind(2)` to. |
| `launchd.agents.<name>.serviceConfig.Sockets.<name>.SockType` | `null or one of "stream", "dgram", "seqpacket"` | This optional key tells launchctl what type of socket to create. The default is "stream" and other valid values for this key are "dgram" and "seqpacket" respectively. |
| `launchd.agents.<name>.serviceConfig.SoftResourceLimits` | `null or (submodule)` | Resource limits to be imposed on the job. These adjust variables set with `setrlimit(2)`. The following keys apply: |
| `launchd.agents.<name>.serviceConfig.SoftResourceLimits.CPU` | `null or signed integer` | The maximum amount of cpu time (in seconds) to be used by each process. |
| `launchd.agents.<name>.serviceConfig.SoftResourceLimits.Core` | `null or signed integer` | The largest size (in bytes) core file that may be created. |
| `launchd.agents.<name>.serviceConfig.SoftResourceLimits.Data` | `null or signed integer` | The maximum size (in bytes) of the data segment for a process; this defines how far a program may extend its break with the `sbrk(2)` system call. |
| `launchd.agents.<name>.serviceConfig.SoftResourceLimits.FileSize` | `null or signed integer` | The largest size (in bytes) file that may be created. |
| `launchd.agents.<name>.serviceConfig.SoftResourceLimits.MemoryLock` | `null or signed integer` | The maximum size (in bytes) which a process may lock into memory using the mlock(2) function. |
| `launchd.agents.<name>.serviceConfig.SoftResourceLimits.NumberOfFiles` | `null or signed integer` | The maximum number of open files for this process. Setting this value in a system wide daemon will set the `sysctl(3)` kern.maxfiles (SoftResourceLimits) or kern.maxfilesperproc (HardResourceLimits) value in addition to the `setrlimit(2)` values. |
| `launchd.agents.<name>.serviceConfig.SoftResourceLimits.NumberOfProcesses` | `null or signed integer` | The maximum number of simultaneous processes for this user id. Setting this value in a system wide daemon will set the `sysctl(3)` kern.maxproc (SoftResourceLimits) or kern.maxprocperuid (HardResourceLimits) value in addition to the `setrlimit(2)` values. |
| `launchd.agents.<name>.serviceConfig.SoftResourceLimits.ResidentSetSize` | `null or signed integer` | The maximum size (in bytes) to which a process's resident set size may grow. This imposes a limit on the amount of physical memory to be given to a process; if memory is tight, the system will prefer to take memory from processes that are exceeding their declared resident set size. |
| `launchd.agents.<name>.serviceConfig.SoftResourceLimits.Stack` | `null or signed integer` | The maximum size (in bytes) of the stack segment for a process; this defines how far a program's stack segment may be extended. Stack extension is performed automatically by the system. |
| `launchd.agents.<name>.serviceConfig.StandardErrorPath` | `null or absolute path` | This optional key specifies what file should be used for data being sent to stderr when using `stdio(3)`. |
| `launchd.agents.<name>.serviceConfig.StandardInPath` | `null or absolute path` | This optional key specifies what file should be used for data being supplied to stdin when using `stdio(3)`. |
| `launchd.agents.<name>.serviceConfig.StandardOutPath` | `null or absolute path` | This optional key specifies what file should be used for data being sent to stdout when using `stdio(3)`. |
| `launchd.agents.<name>.serviceConfig.StartCalendarInterval` | `null or (submodule) or unique (non-empty (list of (submodule)))` | This optional key causes the job to be started every calendar interval as specified. The semantics are much like {manpage}`crontab(5)`: Missing attributes are considered to be wildcard. Unlike cron which skips job invocations when the computer is asleep, launchd will start the job the next time the computer wakes up. If multiple intervals transpire before the computer is woken, those events will be coalesced into one event upon waking from sleep. ::: {.important} The list must not be empty and must not contain duplicate entries (attrsets which compare equally). ::: ::: {.caution} Since missing attrs become wildcards, an empty attrset effectively means "every minute". ::: |
| `launchd.agents.<name>.serviceConfig.StartInterval` | `null or signed integer` | This optional key causes the job to be started every N seconds. If the system is asleep, the job will be started the next time the computer wakes up. If multiple intervals transpire before the computer is woken, those events will be coalesced into one event upon wake from sleep. |
| `launchd.agents.<name>.serviceConfig.StartOnMount` | `null or boolean` | This optional key causes the job to be started every time a filesystem is mounted. |
| `launchd.agents.<name>.serviceConfig.ThrottleInterval` | `null or signed integer` | This key lets one override the default throttling policy imposed on jobs by launchd. The value is in seconds, and by default, jobs will not be spawned more than once every 10 seconds. The principle behind this is that jobs should linger around just in case they are needed again in the near future. This not only reduces the latency of responses, but it encourages developers to amortize the cost of program invocation. |
| `launchd.agents.<name>.serviceConfig.TimeOut` | `null or signed integer` | The recommended idle time out (in seconds) to pass to the job. If no value is specified, a default time out will be supplied by launchd for use by the job at check in time. |
| `launchd.agents.<name>.serviceConfig.Umask` | `null or signed integer` | This optional key specifies what value should be passed to `umask(2)` before running the job. Known bug: Property lists don't support octal, so please convert the value to decimal. |
| `launchd.agents.<name>.serviceConfig.UserName` | `null or string` | This optional key specifies the user to run the job as. This key is only applicable when launchd is running as root. |
| `launchd.agents.<name>.serviceConfig.WaitForDebugger` | `null or boolean` | This optional key specifies that launchd should instruct the kernel to have the job wait for a debugger to attach before any code in the job is executed. |
| `launchd.agents.<name>.serviceConfig.WatchPaths` | `null or (list of absolute path)` | This optional key causes the job to be started if any one of the listed paths are modified. |
| `launchd.agents.<name>.serviceConfig.WorkingDirectory` | `null or string` | This optional key is used to specify a directory to `chdir(2)` to before running the job. |
| `launchd.agents.<name>.serviceConfig.inetdCompatibility` | `null or (submodule)` | The presence of this key specifies that the daemon expects to be run as if it were launched from inetd. |
| `launchd.agents.<name>.serviceConfig.inetdCompatibility.Wait` | `null or boolean or string` | This flag corresponds to the "wait" or "nowait" option of inetd. If true, then the listening socket is passed via the standard in/out/error file descriptors. If false, then `accept(2)` is called on behalf of the job, and the result is passed via the standard in/out/error descriptors. |
| `launchd.daemons` | `attribute set of (submodule)` | Definition of launchd daemons. After the system is booted and the kernel is running, launchd is run to finish the system initialization. As part of that initialization, it goes through the following steps: 1. It loads the parameters for each launch-on-demand system-level daemon from the property list files found in /System/Library/LaunchDaemons/ and /Library/LaunchDaemons/. 2. It registers the sockets and file descriptors requested by those daemons. 3. It launches any daemons that requested to be running all the time. 4. As requests for a particular service arrive, it launches the corresponding daemon and passes the request to it. 5. When the system shuts down, it sends a SIGTERM signal to all of the daemons that it started. |
| `launchd.daemons.<name>.command` | `string or absolute path` | Command executed as the service's main process. |
| `launchd.daemons.<name>.environment` | `attribute set of (string or list of string)` | Environment variables passed to the service's processes. |
| `launchd.daemons.<name>.path` | `list of (absolute path or string)` | Packages added to the service's {env}`PATH` environment variable. Only the {file}`bin` and subdirectories of each package is added. |
| `launchd.daemons.<name>.script` | `strings concatenated with "\n"` | Shell commands executed as the service's main process. |
| `launchd.daemons.<name>.serviceConfig` | `submodule` | Each attribute in this set specifies an option for a key in the plist. <https://developer.apple.com/legacy/library/documentation/Darwin/Reference/ManPages/man5/launchd.plist.5.html> |
| `launchd.daemons.<name>.serviceConfig.AbandonProcessGroup` | `null or boolean` | When a job dies, launchd kills any remaining processes with the same process group ID as the job. Setting this key to true disables that behavior. |
| `launchd.daemons.<name>.serviceConfig.Debug` | `null or boolean` | This optional key specifies that launchd should adjust its log mask temporarily to LOG_DEBUG while dealing with this job. |
| `launchd.daemons.<name>.serviceConfig.Disabled` | `null or boolean` | This optional key is used as a hint to `launchctl(1)` that it should not submit this job to launchd when loading a job or jobs. The value of this key does NOT reflect the current state of the job on the running system. If you wish to know whether a job is loaded in launchd, reading this key from a configuration file yourself is not a sufficient test. You should query launchd for the presence of the job using the `launchctl(1)` list subcommand or use the ServiceManagement framework's `SMJobCopyDictionary()` method. Note that as of Mac OS X v10.6, this key's value in a configuration file conveys a default value, which is changed with the [-w] option of the `launchctl(1)` load and unload subcommands. These subcommands no longer modify the configuration file, so the value displayed in the configuration file is not necessarily the value that `launchctl(1)` will apply. See `launchctl(1)` for more information. Please also be mindful that you should only use this key if the provided on-demand and KeepAlive criteria are insufficient to describe the conditions under which your job needs to run. The cost to have a job loaded in launchd is negligible, so there is no harm in loading a job which only runs once or very rarely. |
| `launchd.daemons.<name>.serviceConfig.EnableGlobbing` | `null or boolean` | This flag causes launchd to use the `glob(3)` mechanism to update the program arguments before invocation. |
| `launchd.daemons.<name>.serviceConfig.EnableTransactions` | `null or boolean` | This flag instructs launchd that the job promises to use `vproc_transaction_begin(3)` and `vproc_transaction_end(3)` to track outstanding transactions that need to be reconciled before the process can safely terminate. If no outstanding transactions are in progress, then launchd is free to send the SIGKILL signal. |
| `launchd.daemons.<name>.serviceConfig.EnvironmentVariables` | `null or (attribute set of string)` | This optional key is used to specify additional environment variables to be set before running the job. |
| `launchd.daemons.<name>.serviceConfig.ExitTimeOut` | `null or signed integer` | The amount of time launchd waits before sending a SIGKILL signal. The default value is 20 seconds. The value zero is interpreted as infinity. |
| `launchd.daemons.<name>.serviceConfig.GroupName` | `null or string` | This optional key specifies the group to run the job as. This key is only applicable when launchd is running as root. If UserName is set and GroupName is not, the the group will be set to the default group of the user. |
| `launchd.daemons.<name>.serviceConfig.HardResourceLimits` | `null or (submodule)` | Resource limits to be imposed on the job. These adjust variables set with `setrlimit(2)`. The following keys apply: |
| `launchd.daemons.<name>.serviceConfig.HardResourceLimits.CPU` | `null or signed integer` | The maximum amount of cpu time (in seconds) to be used by each process. |
| `launchd.daemons.<name>.serviceConfig.HardResourceLimits.Core` | `null or signed integer` | The largest size (in bytes) core file that may be created. |
| `launchd.daemons.<name>.serviceConfig.HardResourceLimits.Data` | `null or signed integer` | The maximum size (in bytes) of the data segment for a process; this defines how far a program may extend its break with the `sbrk(2)` system call. |
| `launchd.daemons.<name>.serviceConfig.HardResourceLimits.FileSize` | `null or signed integer` | The largest size (in bytes) file that may be created. |
| `launchd.daemons.<name>.serviceConfig.HardResourceLimits.MemoryLock` | `null or signed integer` | The maximum size (in bytes) which a process may lock into memory using the `mlock(2)` function. |
| `launchd.daemons.<name>.serviceConfig.HardResourceLimits.NumberOfFiles` | `null or signed integer` | The maximum number of open files for this process. Setting this value in a system wide daemon will set the `sysctl(3)` kern.maxfiles (SoftResourceLimits) or kern.maxfilesperproc (HardResourceLimits) value in addition to the `setrlimit(2)` values. |
| `launchd.daemons.<name>.serviceConfig.HardResourceLimits.NumberOfProcesses` | `null or signed integer` | The maximum number of simultaneous processes for this user id. Setting this value in a system wide daemon will set the `sysctl(3)` kern.maxproc (SoftResourceLimits) or kern.maxprocperuid (HardResourceLimits) value in addition to the `setrlimit(2)` values. |
| `launchd.daemons.<name>.serviceConfig.HardResourceLimits.ResidentSetSize` | `null or signed integer` | The maximum size (in bytes) to which a process's resident set size may grow. This imposes a limit on the amount of physical memory to be given to a process; if memory is tight, the system will prefer to take memory from processes that are exceeding their declared resident set size. |
| `launchd.daemons.<name>.serviceConfig.HardResourceLimits.Stack` | `null or signed integer` | The maximum size (in bytes) of the stack segment for a process; this defines how far a program's stack segment may be extended. Stack extension is performed automatically by the system. |
| `launchd.daemons.<name>.serviceConfig.InitGroups` | `null or boolean` | This optional key specifies whether `initgroups(3)` should be called before running the job. The default is true in 10.5 and false in 10.4. This key will be ignored if the UserName key is not set. |
| `launchd.daemons.<name>.serviceConfig.KeepAlive` | `null or boolean or (submodule)` | This optional key is used to control whether your job is to be kept continuously running or to let demand and conditions control the invocation. The default is false and therefore only demand will start the job. The value may be set to true to unconditionally keep the job alive. Alternatively, a dictionary of conditions may be specified to selectively control whether launchd keeps a job alive or not. If multiple keys are provided, launchd ORs them, thus providing maximum flexibility to the job to refine the logic and stall if necessary. If launchd finds no reason to restart the job, it falls back on demand based invocation. Jobs that exit quickly and frequently when configured to be kept alive will be throttled to converve system resources. |
| `launchd.daemons.<name>.serviceConfig.Label` | `string` | This required key uniquely identifies the job to launchd. |
| `launchd.daemons.<name>.serviceConfig.LaunchEvents` | `null or (attribute set)` | Specifies higher-level event types to be used as launch-on-demand event sources. Each sub-dictionary defines events for a particular event subsystem, such as "com.apple.iokit.matching", which can be used to launch jobs based on the appearance of nodes in the IORegistry. Each dictionary within the sub-dictionary specifies an event descriptor that is specified to each event subsystem. With this key, the job promises to use the xpc_set_event_stream_handler(3) API to consume events. See xpc_events(3) for more details on event sources. |
| `launchd.daemons.<name>.serviceConfig.LaunchOnlyOnce` | `null or boolean` | This optional key specifies whether the job can only be run once and only once. In other words, if the job cannot be safely respawned without a full machine reboot, then set this key to be true. |
| `launchd.daemons.<name>.serviceConfig.LimitLoadFromHosts` | `null or (list of string)` | This configuration file only applies to hosts NOT listed with this key. Note: One should set kern.hostname in `sysctl.conf(5)` for this feature to work reliably. |
| `launchd.daemons.<name>.serviceConfig.LimitLoadToHosts` | `null or (list of string)` | This configuration file only applies to the hosts listed with this key. Note: One should set kern.hostname in `sysctl.conf(5)` for this feature to work reliably. |
| `launchd.daemons.<name>.serviceConfig.LimitLoadToSessionType` | `null or string or list of string` | This configuration file only applies to sessions of the type specified. This key is used in concert with the -S flag to {command}`launchctl`. |
| `launchd.daemons.<name>.serviceConfig.LowPriorityBackgroundIO` | `null or boolean` | This optional key specifies whether the kernel should consider this daemon to be low priority when doing file system I/O when the process is throttled with the Darwin-background classification. |
| `launchd.daemons.<name>.serviceConfig.LowPriorityIO` | `null or boolean` | This optional key specifies whether the kernel should consider this daemon to be low priority when doing file system I/O. |
| `launchd.daemons.<name>.serviceConfig.MachServices` | `null or (attribute set of (boolean or (submodule)))` | This optional key is used to specify Mach services to be registered with the Mach bootstrap sub-system. Each key in this dictionary should be the name of service to be advertised. The value of the key must be a boolean and set to true. Alternatively, a dictionary can be used instead of a simple true value. Finally, for the job itself, the values will be replaced with Mach ports at the time of check-in with launchd. |
| `launchd.daemons.<name>.serviceConfig.Nice` | `null or signed integer` | This optional key specifies what nice(3) value should be applied to the daemon. |
| `launchd.daemons.<name>.serviceConfig.OnDemand` | `null or boolean` | This key was used in Mac OS X 10.4 to control whether a job was kept alive or not. The default was true. This key has been deprecated and replaced in Mac OS X 10.5 and later with the more powerful KeepAlive option. |
| `launchd.daemons.<name>.serviceConfig.ProcessType` | `null or one of "Background", "Standard", "Adaptive", "Interactive"` | This optional key describes, at a high level, the intended purpose of the job. The system will apply resource limits based on what kind of job it is. If left unspecified, the system will apply light resource limits to the job, throttling its CPU usage and I/O bandwidth. The following are valid values: Background : Background jobs are generally processes that do work that was not directly requested by the user. The resource limits applied to Background jobs are intended to prevent them from disrupting the user experience. Standard : Standard jobs are equivalent to no ProcessType being set. Adaptive : Adaptive jobs move between the Background and Interactive classifications based on activity over XPC connections. See `xpc_transaction_begin(3)` for details. Interactive : Interactive jobs run with the same resource limitations as apps, that is to say, none. Interactive jobs are critical to maintaining a responsive user experience, and this key should only be used if an app's ability to be responsive depends on it, and cannot be made Adaptive. |
| `launchd.daemons.<name>.serviceConfig.Program` | `null or absolute path` | This key maps to the first argument of `execvp(3)`. If this key is missing, then the first element of the array of strings provided to the ProgramArguments will be used instead. This key is required in the absence of the ProgramArguments key. |
| `launchd.daemons.<name>.serviceConfig.ProgramArguments` | `null or (list of string)` | This key maps to the second argument of `execvp(3)`. This key is required in the absence of the Program key. Please note: many people are confused by this key. Please read `execvp(3)` very carefully! |
| `launchd.daemons.<name>.serviceConfig.QueueDirectories` | `null or (list of string)` | Much like the WatchPaths option, this key will watch the paths for modifications. The difference being that the job will only be started if the path is a directory and the directory is not empty. |
| `launchd.daemons.<name>.serviceConfig.RootDirectory` | `null or string` | This optional key is used to specify a directory to `chroot(2)` to before running the job. |
| `launchd.daemons.<name>.serviceConfig.RunAtLoad` | `null or boolean` | This optional key is used to control whether your job is launched once at the time the job is loaded. The default is false. |
| `launchd.daemons.<name>.serviceConfig.ServiceIPC` | `null or boolean` | This optional key specifies whether the job participates in advanced communication with launchd. The default is false. This flag is incompatible with the inetdCompatibility key. |
| `launchd.daemons.<name>.serviceConfig.SessionCreate` | `null or boolean` | This key specifies that the job should be spawned into a new security audit session rather than the default session for the context is belongs to. See auditon(2) for details. |
| `launchd.daemons.<name>.serviceConfig.Sockets` | `null or (attribute set of (submodule))` | This optional key is used to specify launch on demand sockets that can be used to let launchd know when to run the job. The job must check-in to get a copy of the file descriptors using APIs outlined in launch(3). The keys of the top level Sockets dictionary can be anything. They are meant for the application developer to use to differentiate which descriptors correspond to which application level protocols (e.g. http vs. ftp vs. DNS...). At check-in time, the value of each Sockets dictionary key will be an array of descriptors. Daemon/Agent writers should consider all descriptors of a given key to be to be effectively equivalent, even though each file descriptor likely represents a different networking protocol which conforms to the criteria specified in the job configuration file. The parameters below are used as inputs to call `getaddrinfo(3)`. |
| `launchd.daemons.<name>.serviceConfig.Sockets.<name>.Bonjour` | `null or boolean or list of string` | This optional key can be used to request that the service be registered with the `mDNSResponder(8)`. If the value is boolean, the service name is inferred from the SockServiceName. |
| `launchd.daemons.<name>.serviceConfig.Sockets.<name>.MulticastGroup` | `null or string` | This optional key can be used to request that the datagram socket join a multicast group. If the value is a hostname, then `getaddrinfo(3)` will be used to join the correct multicast address for a given socket family. If an explicit IPv4 or IPv6 address is given, it is required that the SockFamily family also be set, otherwise the results are undefined. |
| `launchd.daemons.<name>.serviceConfig.Sockets.<name>.SecureSocketWithKey` | `null or string` | This optional key is a variant of SockPathName. Instead of binding to a known path, a securely generated socket is created and the path is assigned to the environment variable that is inherited by all jobs spawned by launchd. |
| `launchd.daemons.<name>.serviceConfig.Sockets.<name>.SockFamily` | `null or one of "IPv4", "IPv6"` | This optional key can be used to specifically request that "IPv4" or "IPv6" socket(s) be created. |
| `launchd.daemons.<name>.serviceConfig.Sockets.<name>.SockNodeName` | `null or string` | This optional key specifies the node to `connect(2)` or `bind(2)` to. |
| `launchd.daemons.<name>.serviceConfig.Sockets.<name>.SockPassive` | `null or boolean` | This optional key specifies whether `listen(2)` or `connect(2)` should be called on the created file descriptor. The default is true ("to listen"). |
| `launchd.daemons.<name>.serviceConfig.Sockets.<name>.SockPathMode` | `null or signed integer` | This optional key specifies the mode of the socket. Known bug: Property lists don't support octal, so please convert the value to decimal. |
| `launchd.daemons.<name>.serviceConfig.Sockets.<name>.SockPathName` | `null or absolute path` | This optional key implies SockFamily is set to "Unix". It specifies the path to `connect(2)` or `bind(2)` to. |
| `launchd.daemons.<name>.serviceConfig.Sockets.<name>.SockProtocol` | `null or value "TCP" (singular enum)` | This optional key specifies the protocol to be passed to `socket(2)`. The only value understood by this key at the moment is "TCP". |
| `launchd.daemons.<name>.serviceConfig.Sockets.<name>.SockServiceName` | `null or string` | This optional key specifies the service on the node to `connect(2)` or `bind(2)` to. |
| `launchd.daemons.<name>.serviceConfig.Sockets.<name>.SockType` | `null or one of "stream", "dgram", "seqpacket"` | This optional key tells launchctl what type of socket to create. The default is "stream" and other valid values for this key are "dgram" and "seqpacket" respectively. |
| `launchd.daemons.<name>.serviceConfig.SoftResourceLimits` | `null or (submodule)` | Resource limits to be imposed on the job. These adjust variables set with `setrlimit(2)`. The following keys apply: |
| `launchd.daemons.<name>.serviceConfig.SoftResourceLimits.CPU` | `null or signed integer` | The maximum amount of cpu time (in seconds) to be used by each process. |
| `launchd.daemons.<name>.serviceConfig.SoftResourceLimits.Core` | `null or signed integer` | The largest size (in bytes) core file that may be created. |
| `launchd.daemons.<name>.serviceConfig.SoftResourceLimits.Data` | `null or signed integer` | The maximum size (in bytes) of the data segment for a process; this defines how far a program may extend its break with the `sbrk(2)` system call. |
| `launchd.daemons.<name>.serviceConfig.SoftResourceLimits.FileSize` | `null or signed integer` | The largest size (in bytes) file that may be created. |
| `launchd.daemons.<name>.serviceConfig.SoftResourceLimits.MemoryLock` | `null or signed integer` | The maximum size (in bytes) which a process may lock into memory using the mlock(2) function. |
| `launchd.daemons.<name>.serviceConfig.SoftResourceLimits.NumberOfFiles` | `null or signed integer` | The maximum number of open files for this process. Setting this value in a system wide daemon will set the `sysctl(3)` kern.maxfiles (SoftResourceLimits) or kern.maxfilesperproc (HardResourceLimits) value in addition to the `setrlimit(2)` values. |
| `launchd.daemons.<name>.serviceConfig.SoftResourceLimits.NumberOfProcesses` | `null or signed integer` | The maximum number of simultaneous processes for this user id. Setting this value in a system wide daemon will set the `sysctl(3)` kern.maxproc (SoftResourceLimits) or kern.maxprocperuid (HardResourceLimits) value in addition to the `setrlimit(2)` values. |
| `launchd.daemons.<name>.serviceConfig.SoftResourceLimits.ResidentSetSize` | `null or signed integer` | The maximum size (in bytes) to which a process's resident set size may grow. This imposes a limit on the amount of physical memory to be given to a process; if memory is tight, the system will prefer to take memory from processes that are exceeding their declared resident set size. |
| `launchd.daemons.<name>.serviceConfig.SoftResourceLimits.Stack` | `null or signed integer` | The maximum size (in bytes) of the stack segment for a process; this defines how far a program's stack segment may be extended. Stack extension is performed automatically by the system. |
| `launchd.daemons.<name>.serviceConfig.StandardErrorPath` | `null or absolute path` | This optional key specifies what file should be used for data being sent to stderr when using `stdio(3)`. |
| `launchd.daemons.<name>.serviceConfig.StandardInPath` | `null or absolute path` | This optional key specifies what file should be used for data being supplied to stdin when using `stdio(3)`. |
| `launchd.daemons.<name>.serviceConfig.StandardOutPath` | `null or absolute path` | This optional key specifies what file should be used for data being sent to stdout when using `stdio(3)`. |
| `launchd.daemons.<name>.serviceConfig.StartCalendarInterval` | `null or (submodule) or unique (non-empty (list of (submodule)))` | This optional key causes the job to be started every calendar interval as specified. The semantics are much like {manpage}`crontab(5)`: Missing attributes are considered to be wildcard. Unlike cron which skips job invocations when the computer is asleep, launchd will start the job the next time the computer wakes up. If multiple intervals transpire before the computer is woken, those events will be coalesced into one event upon waking from sleep. ::: {.important} The list must not be empty and must not contain duplicate entries (attrsets which compare equally). ::: ::: {.caution} Since missing attrs become wildcards, an empty attrset effectively means "every minute". ::: |
| `launchd.daemons.<name>.serviceConfig.StartInterval` | `null or signed integer` | This optional key causes the job to be started every N seconds. If the system is asleep, the job will be started the next time the computer wakes up. If multiple intervals transpire before the computer is woken, those events will be coalesced into one event upon wake from sleep. |
| `launchd.daemons.<name>.serviceConfig.StartOnMount` | `null or boolean` | This optional key causes the job to be started every time a filesystem is mounted. |
| `launchd.daemons.<name>.serviceConfig.ThrottleInterval` | `null or signed integer` | This key lets one override the default throttling policy imposed on jobs by launchd. The value is in seconds, and by default, jobs will not be spawned more than once every 10 seconds. The principle behind this is that jobs should linger around just in case they are needed again in the near future. This not only reduces the latency of responses, but it encourages developers to amortize the cost of program invocation. |
| `launchd.daemons.<name>.serviceConfig.TimeOut` | `null or signed integer` | The recommended idle time out (in seconds) to pass to the job. If no value is specified, a default time out will be supplied by launchd for use by the job at check in time. |
| `launchd.daemons.<name>.serviceConfig.Umask` | `null or signed integer` | This optional key specifies what value should be passed to `umask(2)` before running the job. Known bug: Property lists don't support octal, so please convert the value to decimal. |
| `launchd.daemons.<name>.serviceConfig.UserName` | `null or string` | This optional key specifies the user to run the job as. This key is only applicable when launchd is running as root. |
| `launchd.daemons.<name>.serviceConfig.WaitForDebugger` | `null or boolean` | This optional key specifies that launchd should instruct the kernel to have the job wait for a debugger to attach before any code in the job is executed. |
| `launchd.daemons.<name>.serviceConfig.WatchPaths` | `null or (list of absolute path)` | This optional key causes the job to be started if any one of the listed paths are modified. |
| `launchd.daemons.<name>.serviceConfig.WorkingDirectory` | `null or string` | This optional key is used to specify a directory to `chdir(2)` to before running the job. |
| `launchd.daemons.<name>.serviceConfig.inetdCompatibility` | `null or (submodule)` | The presence of this key specifies that the daemon expects to be run as if it were launched from inetd. |
| `launchd.daemons.<name>.serviceConfig.inetdCompatibility.Wait` | `null or boolean or string` | This flag corresponds to the "wait" or "nowait" option of inetd. If true, then the listening socket is passed via the standard in/out/error file descriptors. If false, then `accept(2)` is called on behalf of the job, and the result is passed via the standard in/out/error descriptors. |
| `launchd.envVariables` | `attribute set of (string or list of string)` | A set of environment variables to be set on all future processes launched by launchd in the caller's context. The value of each variable can be either a string or a list of strings. The latter is concatenated, interspersed with colon characters. |
| `launchd.labelPrefix` | `string` | The default prefix of the service label. Individual services can override this by setting the Label attribute. |
| `launchd.user.agents` | `attribute set of (submodule)` | Definition of per-user launchd agents. When a user logs in, a per-user launchd is started. It does the following: 1. It loads the parameters for each launch-on-demand user agent from the property list files found in /System/Library/LaunchAgents, /Library/LaunchAgents, and the user’s individual Library/LaunchAgents directory. 2. It registers the sockets and file descriptors requested by those user agents. 3. It launches any user agents that requested to be running all the time. 4. As requests for a particular service arrive, it launches the corresponding user agent and passes the request to it. 5. When the user logs out, it sends a SIGTERM signal to all of the user agents that it started. |
| `launchd.user.agents.<name>.command` | `string or absolute path` | Command executed as the service's main process. |
| `launchd.user.agents.<name>.environment` | `attribute set of (string or list of string)` | Environment variables passed to the service's processes. |
| `launchd.user.agents.<name>.path` | `list of (absolute path or string)` | Packages added to the service's {env}`PATH` environment variable. Only the {file}`bin` and subdirectories of each package is added. |
| `launchd.user.agents.<name>.script` | `strings concatenated with "\n"` | Shell commands executed as the service's main process. |
| `launchd.user.agents.<name>.serviceConfig` | `submodule` | Each attribute in this set specifies an option for a key in the plist. <https://developer.apple.com/legacy/library/documentation/Darwin/Reference/ManPages/man5/launchd.plist.5.html> |
| `launchd.user.agents.<name>.serviceConfig.AbandonProcessGroup` | `null or boolean` | When a job dies, launchd kills any remaining processes with the same process group ID as the job. Setting this key to true disables that behavior. |
| `launchd.user.agents.<name>.serviceConfig.Debug` | `null or boolean` | This optional key specifies that launchd should adjust its log mask temporarily to LOG_DEBUG while dealing with this job. |
| `launchd.user.agents.<name>.serviceConfig.Disabled` | `null or boolean` | This optional key is used as a hint to `launchctl(1)` that it should not submit this job to launchd when loading a job or jobs. The value of this key does NOT reflect the current state of the job on the running system. If you wish to know whether a job is loaded in launchd, reading this key from a configuration file yourself is not a sufficient test. You should query launchd for the presence of the job using the `launchctl(1)` list subcommand or use the ServiceManagement framework's `SMJobCopyDictionary()` method. Note that as of Mac OS X v10.6, this key's value in a configuration file conveys a default value, which is changed with the [-w] option of the `launchctl(1)` load and unload subcommands. These subcommands no longer modify the configuration file, so the value displayed in the configuration file is not necessarily the value that `launchctl(1)` will apply. See `launchctl(1)` for more information. Please also be mindful that you should only use this key if the provided on-demand and KeepAlive criteria are insufficient to describe the conditions under which your job needs to run. The cost to have a job loaded in launchd is negligible, so there is no harm in loading a job which only runs once or very rarely. |
| `launchd.user.agents.<name>.serviceConfig.EnableGlobbing` | `null or boolean` | This flag causes launchd to use the `glob(3)` mechanism to update the program arguments before invocation. |
| `launchd.user.agents.<name>.serviceConfig.EnableTransactions` | `null or boolean` | This flag instructs launchd that the job promises to use `vproc_transaction_begin(3)` and `vproc_transaction_end(3)` to track outstanding transactions that need to be reconciled before the process can safely terminate. If no outstanding transactions are in progress, then launchd is free to send the SIGKILL signal. |
| `launchd.user.agents.<name>.serviceConfig.EnvironmentVariables` | `null or (attribute set of string)` | This optional key is used to specify additional environment variables to be set before running the job. |
| `launchd.user.agents.<name>.serviceConfig.ExitTimeOut` | `null or signed integer` | The amount of time launchd waits before sending a SIGKILL signal. The default value is 20 seconds. The value zero is interpreted as infinity. |
| `launchd.user.agents.<name>.serviceConfig.GroupName` | `null or string` | This optional key specifies the group to run the job as. This key is only applicable when launchd is running as root. If UserName is set and GroupName is not, the the group will be set to the default group of the user. |
| `launchd.user.agents.<name>.serviceConfig.HardResourceLimits` | `null or (submodule)` | Resource limits to be imposed on the job. These adjust variables set with `setrlimit(2)`. The following keys apply: |
| `launchd.user.agents.<name>.serviceConfig.HardResourceLimits.CPU` | `null or signed integer` | The maximum amount of cpu time (in seconds) to be used by each process. |
| `launchd.user.agents.<name>.serviceConfig.HardResourceLimits.Core` | `null or signed integer` | The largest size (in bytes) core file that may be created. |
| `launchd.user.agents.<name>.serviceConfig.HardResourceLimits.Data` | `null or signed integer` | The maximum size (in bytes) of the data segment for a process; this defines how far a program may extend its break with the `sbrk(2)` system call. |
| `launchd.user.agents.<name>.serviceConfig.HardResourceLimits.FileSize` | `null or signed integer` | The largest size (in bytes) file that may be created. |
| `launchd.user.agents.<name>.serviceConfig.HardResourceLimits.MemoryLock` | `null or signed integer` | The maximum size (in bytes) which a process may lock into memory using the `mlock(2)` function. |
| `launchd.user.agents.<name>.serviceConfig.HardResourceLimits.NumberOfFiles` | `null or signed integer` | The maximum number of open files for this process. Setting this value in a system wide daemon will set the `sysctl(3)` kern.maxfiles (SoftResourceLimits) or kern.maxfilesperproc (HardResourceLimits) value in addition to the `setrlimit(2)` values. |
| `launchd.user.agents.<name>.serviceConfig.HardResourceLimits.NumberOfProcesses` | `null or signed integer` | The maximum number of simultaneous processes for this user id. Setting this value in a system wide daemon will set the `sysctl(3)` kern.maxproc (SoftResourceLimits) or kern.maxprocperuid (HardResourceLimits) value in addition to the `setrlimit(2)` values. |
| `launchd.user.agents.<name>.serviceConfig.HardResourceLimits.ResidentSetSize` | `null or signed integer` | The maximum size (in bytes) to which a process's resident set size may grow. This imposes a limit on the amount of physical memory to be given to a process; if memory is tight, the system will prefer to take memory from processes that are exceeding their declared resident set size. |
| `launchd.user.agents.<name>.serviceConfig.HardResourceLimits.Stack` | `null or signed integer` | The maximum size (in bytes) of the stack segment for a process; this defines how far a program's stack segment may be extended. Stack extension is performed automatically by the system. |
| `launchd.user.agents.<name>.serviceConfig.InitGroups` | `null or boolean` | This optional key specifies whether `initgroups(3)` should be called before running the job. The default is true in 10.5 and false in 10.4. This key will be ignored if the UserName key is not set. |
| `launchd.user.agents.<name>.serviceConfig.KeepAlive` | `null or boolean or (submodule)` | This optional key is used to control whether your job is to be kept continuously running or to let demand and conditions control the invocation. The default is false and therefore only demand will start the job. The value may be set to true to unconditionally keep the job alive. Alternatively, a dictionary of conditions may be specified to selectively control whether launchd keeps a job alive or not. If multiple keys are provided, launchd ORs them, thus providing maximum flexibility to the job to refine the logic and stall if necessary. If launchd finds no reason to restart the job, it falls back on demand based invocation. Jobs that exit quickly and frequently when configured to be kept alive will be throttled to converve system resources. |
| `launchd.user.agents.<name>.serviceConfig.Label` | `string` | This required key uniquely identifies the job to launchd. |
| `launchd.user.agents.<name>.serviceConfig.LaunchEvents` | `null or (attribute set)` | Specifies higher-level event types to be used as launch-on-demand event sources. Each sub-dictionary defines events for a particular event subsystem, such as "com.apple.iokit.matching", which can be used to launch jobs based on the appearance of nodes in the IORegistry. Each dictionary within the sub-dictionary specifies an event descriptor that is specified to each event subsystem. With this key, the job promises to use the xpc_set_event_stream_handler(3) API to consume events. See xpc_events(3) for more details on event sources. |
| `launchd.user.agents.<name>.serviceConfig.LaunchOnlyOnce` | `null or boolean` | This optional key specifies whether the job can only be run once and only once. In other words, if the job cannot be safely respawned without a full machine reboot, then set this key to be true. |
| `launchd.user.agents.<name>.serviceConfig.LimitLoadFromHosts` | `null or (list of string)` | This configuration file only applies to hosts NOT listed with this key. Note: One should set kern.hostname in `sysctl.conf(5)` for this feature to work reliably. |
| `launchd.user.agents.<name>.serviceConfig.LimitLoadToHosts` | `null or (list of string)` | This configuration file only applies to the hosts listed with this key. Note: One should set kern.hostname in `sysctl.conf(5)` for this feature to work reliably. |
| `launchd.user.agents.<name>.serviceConfig.LimitLoadToSessionType` | `null or string or list of string` | This configuration file only applies to sessions of the type specified. This key is used in concert with the -S flag to {command}`launchctl`. |
| `launchd.user.agents.<name>.serviceConfig.LowPriorityBackgroundIO` | `null or boolean` | This optional key specifies whether the kernel should consider this daemon to be low priority when doing file system I/O when the process is throttled with the Darwin-background classification. |
| `launchd.user.agents.<name>.serviceConfig.LowPriorityIO` | `null or boolean` | This optional key specifies whether the kernel should consider this daemon to be low priority when doing file system I/O. |
| `launchd.user.agents.<name>.serviceConfig.MachServices` | `null or (attribute set of (boolean or (submodule)))` | This optional key is used to specify Mach services to be registered with the Mach bootstrap sub-system. Each key in this dictionary should be the name of service to be advertised. The value of the key must be a boolean and set to true. Alternatively, a dictionary can be used instead of a simple true value. Finally, for the job itself, the values will be replaced with Mach ports at the time of check-in with launchd. |
| `launchd.user.agents.<name>.serviceConfig.Nice` | `null or signed integer` | This optional key specifies what nice(3) value should be applied to the daemon. |
| `launchd.user.agents.<name>.serviceConfig.OnDemand` | `null or boolean` | This key was used in Mac OS X 10.4 to control whether a job was kept alive or not. The default was true. This key has been deprecated and replaced in Mac OS X 10.5 and later with the more powerful KeepAlive option. |
| `launchd.user.agents.<name>.serviceConfig.ProcessType` | `null or one of "Background", "Standard", "Adaptive", "Interactive"` | This optional key describes, at a high level, the intended purpose of the job. The system will apply resource limits based on what kind of job it is. If left unspecified, the system will apply light resource limits to the job, throttling its CPU usage and I/O bandwidth. The following are valid values: Background : Background jobs are generally processes that do work that was not directly requested by the user. The resource limits applied to Background jobs are intended to prevent them from disrupting the user experience. Standard : Standard jobs are equivalent to no ProcessType being set. Adaptive : Adaptive jobs move between the Background and Interactive classifications based on activity over XPC connections. See `xpc_transaction_begin(3)` for details. Interactive : Interactive jobs run with the same resource limitations as apps, that is to say, none. Interactive jobs are critical to maintaining a responsive user experience, and this key should only be used if an app's ability to be responsive depends on it, and cannot be made Adaptive. |
| `launchd.user.agents.<name>.serviceConfig.Program` | `null or absolute path` | This key maps to the first argument of `execvp(3)`. If this key is missing, then the first element of the array of strings provided to the ProgramArguments will be used instead. This key is required in the absence of the ProgramArguments key. |
| `launchd.user.agents.<name>.serviceConfig.ProgramArguments` | `null or (list of string)` | This key maps to the second argument of `execvp(3)`. This key is required in the absence of the Program key. Please note: many people are confused by this key. Please read `execvp(3)` very carefully! |
| `launchd.user.agents.<name>.serviceConfig.QueueDirectories` | `null or (list of string)` | Much like the WatchPaths option, this key will watch the paths for modifications. The difference being that the job will only be started if the path is a directory and the directory is not empty. |
| `launchd.user.agents.<name>.serviceConfig.RootDirectory` | `null or string` | This optional key is used to specify a directory to `chroot(2)` to before running the job. |
| `launchd.user.agents.<name>.serviceConfig.RunAtLoad` | `null or boolean` | This optional key is used to control whether your job is launched once at the time the job is loaded. The default is false. |
| `launchd.user.agents.<name>.serviceConfig.ServiceIPC` | `null or boolean` | This optional key specifies whether the job participates in advanced communication with launchd. The default is false. This flag is incompatible with the inetdCompatibility key. |
| `launchd.user.agents.<name>.serviceConfig.SessionCreate` | `null or boolean` | This key specifies that the job should be spawned into a new security audit session rather than the default session for the context is belongs to. See auditon(2) for details. |
| `launchd.user.agents.<name>.serviceConfig.Sockets` | `null or (attribute set of (submodule))` | This optional key is used to specify launch on demand sockets that can be used to let launchd know when to run the job. The job must check-in to get a copy of the file descriptors using APIs outlined in launch(3). The keys of the top level Sockets dictionary can be anything. They are meant for the application developer to use to differentiate which descriptors correspond to which application level protocols (e.g. http vs. ftp vs. DNS...). At check-in time, the value of each Sockets dictionary key will be an array of descriptors. Daemon/Agent writers should consider all descriptors of a given key to be to be effectively equivalent, even though each file descriptor likely represents a different networking protocol which conforms to the criteria specified in the job configuration file. The parameters below are used as inputs to call `getaddrinfo(3)`. |
| `launchd.user.agents.<name>.serviceConfig.Sockets.<name>.Bonjour` | `null or boolean or list of string` | This optional key can be used to request that the service be registered with the `mDNSResponder(8)`. If the value is boolean, the service name is inferred from the SockServiceName. |
| `launchd.user.agents.<name>.serviceConfig.Sockets.<name>.MulticastGroup` | `null or string` | This optional key can be used to request that the datagram socket join a multicast group. If the value is a hostname, then `getaddrinfo(3)` will be used to join the correct multicast address for a given socket family. If an explicit IPv4 or IPv6 address is given, it is required that the SockFamily family also be set, otherwise the results are undefined. |
| `launchd.user.agents.<name>.serviceConfig.Sockets.<name>.SecureSocketWithKey` | `null or string` | This optional key is a variant of SockPathName. Instead of binding to a known path, a securely generated socket is created and the path is assigned to the environment variable that is inherited by all jobs spawned by launchd. |
| `launchd.user.agents.<name>.serviceConfig.Sockets.<name>.SockFamily` | `null or one of "IPv4", "IPv6"` | This optional key can be used to specifically request that "IPv4" or "IPv6" socket(s) be created. |
| `launchd.user.agents.<name>.serviceConfig.Sockets.<name>.SockNodeName` | `null or string` | This optional key specifies the node to `connect(2)` or `bind(2)` to. |
| `launchd.user.agents.<name>.serviceConfig.Sockets.<name>.SockPassive` | `null or boolean` | This optional key specifies whether `listen(2)` or `connect(2)` should be called on the created file descriptor. The default is true ("to listen"). |
| `launchd.user.agents.<name>.serviceConfig.Sockets.<name>.SockPathMode` | `null or signed integer` | This optional key specifies the mode of the socket. Known bug: Property lists don't support octal, so please convert the value to decimal. |
| `launchd.user.agents.<name>.serviceConfig.Sockets.<name>.SockPathName` | `null or absolute path` | This optional key implies SockFamily is set to "Unix". It specifies the path to `connect(2)` or `bind(2)` to. |
| `launchd.user.agents.<name>.serviceConfig.Sockets.<name>.SockProtocol` | `null or value "TCP" (singular enum)` | This optional key specifies the protocol to be passed to `socket(2)`. The only value understood by this key at the moment is "TCP". |
| `launchd.user.agents.<name>.serviceConfig.Sockets.<name>.SockServiceName` | `null or string` | This optional key specifies the service on the node to `connect(2)` or `bind(2)` to. |
| `launchd.user.agents.<name>.serviceConfig.Sockets.<name>.SockType` | `null or one of "stream", "dgram", "seqpacket"` | This optional key tells launchctl what type of socket to create. The default is "stream" and other valid values for this key are "dgram" and "seqpacket" respectively. |
| `launchd.user.agents.<name>.serviceConfig.SoftResourceLimits` | `null or (submodule)` | Resource limits to be imposed on the job. These adjust variables set with `setrlimit(2)`. The following keys apply: |
| `launchd.user.agents.<name>.serviceConfig.SoftResourceLimits.CPU` | `null or signed integer` | The maximum amount of cpu time (in seconds) to be used by each process. |
| `launchd.user.agents.<name>.serviceConfig.SoftResourceLimits.Core` | `null or signed integer` | The largest size (in bytes) core file that may be created. |
| `launchd.user.agents.<name>.serviceConfig.SoftResourceLimits.Data` | `null or signed integer` | The maximum size (in bytes) of the data segment for a process; this defines how far a program may extend its break with the `sbrk(2)` system call. |
| `launchd.user.agents.<name>.serviceConfig.SoftResourceLimits.FileSize` | `null or signed integer` | The largest size (in bytes) file that may be created. |
| `launchd.user.agents.<name>.serviceConfig.SoftResourceLimits.MemoryLock` | `null or signed integer` | The maximum size (in bytes) which a process may lock into memory using the mlock(2) function. |
| `launchd.user.agents.<name>.serviceConfig.SoftResourceLimits.NumberOfFiles` | `null or signed integer` | The maximum number of open files for this process. Setting this value in a system wide daemon will set the `sysctl(3)` kern.maxfiles (SoftResourceLimits) or kern.maxfilesperproc (HardResourceLimits) value in addition to the `setrlimit(2)` values. |
| `launchd.user.agents.<name>.serviceConfig.SoftResourceLimits.NumberOfProcesses` | `null or signed integer` | The maximum number of simultaneous processes for this user id. Setting this value in a system wide daemon will set the `sysctl(3)` kern.maxproc (SoftResourceLimits) or kern.maxprocperuid (HardResourceLimits) value in addition to the `setrlimit(2)` values. |
| `launchd.user.agents.<name>.serviceConfig.SoftResourceLimits.ResidentSetSize` | `null or signed integer` | The maximum size (in bytes) to which a process's resident set size may grow. This imposes a limit on the amount of physical memory to be given to a process; if memory is tight, the system will prefer to take memory from processes that are exceeding their declared resident set size. |
| `launchd.user.agents.<name>.serviceConfig.SoftResourceLimits.Stack` | `null or signed integer` | The maximum size (in bytes) of the stack segment for a process; this defines how far a program's stack segment may be extended. Stack extension is performed automatically by the system. |
| `launchd.user.agents.<name>.serviceConfig.StandardErrorPath` | `null or absolute path` | This optional key specifies what file should be used for data being sent to stderr when using `stdio(3)`. |
| `launchd.user.agents.<name>.serviceConfig.StandardInPath` | `null or absolute path` | This optional key specifies what file should be used for data being supplied to stdin when using `stdio(3)`. |
| `launchd.user.agents.<name>.serviceConfig.StandardOutPath` | `null or absolute path` | This optional key specifies what file should be used for data being sent to stdout when using `stdio(3)`. |
| `launchd.user.agents.<name>.serviceConfig.StartCalendarInterval` | `null or (submodule) or unique (non-empty (list of (submodule)))` | This optional key causes the job to be started every calendar interval as specified. The semantics are much like {manpage}`crontab(5)`: Missing attributes are considered to be wildcard. Unlike cron which skips job invocations when the computer is asleep, launchd will start the job the next time the computer wakes up. If multiple intervals transpire before the computer is woken, those events will be coalesced into one event upon waking from sleep. ::: {.important} The list must not be empty and must not contain duplicate entries (attrsets which compare equally). ::: ::: {.caution} Since missing attrs become wildcards, an empty attrset effectively means "every minute". ::: |
| `launchd.user.agents.<name>.serviceConfig.StartInterval` | `null or signed integer` | This optional key causes the job to be started every N seconds. If the system is asleep, the job will be started the next time the computer wakes up. If multiple intervals transpire before the computer is woken, those events will be coalesced into one event upon wake from sleep. |
| `launchd.user.agents.<name>.serviceConfig.StartOnMount` | `null or boolean` | This optional key causes the job to be started every time a filesystem is mounted. |
| `launchd.user.agents.<name>.serviceConfig.ThrottleInterval` | `null or signed integer` | This key lets one override the default throttling policy imposed on jobs by launchd. The value is in seconds, and by default, jobs will not be spawned more than once every 10 seconds. The principle behind this is that jobs should linger around just in case they are needed again in the near future. This not only reduces the latency of responses, but it encourages developers to amortize the cost of program invocation. |
| `launchd.user.agents.<name>.serviceConfig.TimeOut` | `null or signed integer` | The recommended idle time out (in seconds) to pass to the job. If no value is specified, a default time out will be supplied by launchd for use by the job at check in time. |
| `launchd.user.agents.<name>.serviceConfig.Umask` | `null or signed integer` | This optional key specifies what value should be passed to `umask(2)` before running the job. Known bug: Property lists don't support octal, so please convert the value to decimal. |
| `launchd.user.agents.<name>.serviceConfig.UserName` | `null or string` | This optional key specifies the user to run the job as. This key is only applicable when launchd is running as root. |
| `launchd.user.agents.<name>.serviceConfig.WaitForDebugger` | `null or boolean` | This optional key specifies that launchd should instruct the kernel to have the job wait for a debugger to attach before any code in the job is executed. |
| `launchd.user.agents.<name>.serviceConfig.WatchPaths` | `null or (list of absolute path)` | This optional key causes the job to be started if any one of the listed paths are modified. |
| `launchd.user.agents.<name>.serviceConfig.WorkingDirectory` | `null or string` | This optional key is used to specify a directory to `chdir(2)` to before running the job. |
| `launchd.user.agents.<name>.serviceConfig.inetdCompatibility` | `null or (submodule)` | The presence of this key specifies that the daemon expects to be run as if it were launched from inetd. |
| `launchd.user.agents.<name>.serviceConfig.inetdCompatibility.Wait` | `null or boolean or string` | This flag corresponds to the "wait" or "nowait" option of inetd. If true, then the listening socket is passed via the standard in/out/error file descriptors. If false, then `accept(2)` is called on behalf of the job, and the result is passed via the standard in/out/error descriptors. |
| `launchd.user.envVariables` | `attribute set of (string or list of string)` | A set of environment variables to be set on all future processes launched by launchd in the caller's context. The value of each variable can be either a string or a list of strings. The latter is concatenated, interspersed with colon characters. |
