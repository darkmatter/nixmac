<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Disable sandbox for full system access (required for nix/darwin-rebuild) -->
    <key>com.apple.security.app-sandbox</key>
    <false/>

    <!-- File access -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    <key>com.apple.security.files.downloads.read-write</key>
    <true/>

    <!-- Network access for OpenAI API -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- No com.apple.security.cs.* keys: nothing in the bundle loads unsigned
         or foreign-team libraries (spawning child processes needs no
         entitlement), and keeping library validation active lets the helper's
         dynamic validation of this app also prove no foreign code is loaded
         in-process. -->
</dict>
</plist>
