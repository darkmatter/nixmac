# DO NOT MERGE

This draft PR intentionally breaks the settings button accessibility label so the
nixmac E2E PR gate can prove the failure reporting path:

- failing PR status
- text failure summary
- embedded screenshot
- linked webview flow recording

Delete this branch/PR after validation.
